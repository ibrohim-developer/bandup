// Telegram code input is disabled — Supabase has been removed.
// Re-enable when Telegram auth is implemented with Strapi.

interface TelegramCodeInputProps {
  botName: string
}

export function TelegramCodeInput({ botName: _botName }: TelegramCodeInputProps) {
  return null
}
