{
  "name": "bandup",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n next,strapi -c blue,magenta \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:next": "npm run dev --prefix frontend",
    "dev:strapi": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
